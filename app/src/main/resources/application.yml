server:
  port: 8000

spring:
  application:
    name: com.jlearn
  profiles:
    active: dev
  jackson:
    time-zone: GMT+8

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true

